// alert('HelIo everyone!');

const myOwnArray = [1, 2, 3, 4, 5];

// function showNumber() {
//   for (let index = 0; index < myOwnArray.length; index++) {
//     const element = myOwnArray[index];
//     alert(element);
//   }
// }
//  ===
function showNumber() {
    for (let i = 0; i < myOwnArray.length; i++) {
        // const element = myOwnArray[i];
        // alert(element);
        // console.log(element);
        // === 
        // (element === el)
        const el = myOwnArray[i];
        alert(el);
        console.log(el);
    }
}
showNumber();
